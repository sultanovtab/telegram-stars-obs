{
  "name": "telegram-stars-obs",
  "version": "5.0.0",
  "private": true,
  "description": "Telegram Stars alerts for OBS/TikTok — starchik v5 hardened",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --check src/v4-logic.js && node --check src/overlay-route.js",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm test && npm run check"
  },
  "devDependencies": {
    "wrangler": "4.127.1"
  }
}
